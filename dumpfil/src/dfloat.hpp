#include <vector>
#include <string>

double cvt_d2t(const double&);
void cvt_d2t(char*, int);
void cvt_d2t_multi(char*, std::vector<int>&);
int ana_dflt(const double&);
int ana_dflt(const char*, int);
double ana_dflt(const char*, std::vector<int>&);
